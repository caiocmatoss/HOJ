import { Ionicons } from "@expo/vector-icons";
import { Pressable, StyleSheet, Text, View } from "react-native";
import type { NearbyDiscoveryItem } from "./NearbyDiscoveryCard";
import { colors, fonts } from "@/theme/tokens";
export function FigmaNearbyCard({ item, onPress }: { item: NearbyDiscoveryItem; onPress: () => void }) { const meta = [item.category, item.distance, item.occupancy ?? item.detail].filter(Boolean).join(" · "); return <Pressable accessibilityRole="button" accessibilityLabel={item.title} onPress={onPress} style={({ pressed }) => [styles.card, pressed && styles.pressed]}><View style={[styles.dot, item.kind === "event" && styles.event]} /><View style={styles.copy}><Text numberOfLines={1} style={styles.title}>{item.title}</Text><Text numberOfLines={1} style={styles.meta}>{meta}</Text></View><Ionicons color={colors.textMuted} name="chevron-forward" size={16} /></Pressable>; }
const styles = StyleSheet.create({ card: { alignItems: "center", backgroundColor: colors.surface, borderColor: "rgba(248,250,252,0.05)", borderRadius: 14, borderWidth: 1, flexDirection: "row", gap: 12, minHeight: 64, paddingHorizontal: 14, paddingVertical: 11, width: "100%" }, dot: { backgroundColor: colors.brand, borderRadius: 4, height: 8, width: 8 }, event: { backgroundColor: colors.success }, copy: { flex: 1, minWidth: 0 }, title: { color: colors.text, fontFamily: fonts.semibold, fontSize: 14 }, meta: { color: colors.textMuted, fontFamily: fonts.regular, fontSize: 12, marginTop: 1 }, pressed: { opacity: 0.76 } });
