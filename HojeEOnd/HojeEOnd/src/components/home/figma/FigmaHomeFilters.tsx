import { Pressable, ScrollView, StyleSheet, Text } from "react-native";
import { colors, fonts } from "@/theme/tokens";
export type HomeFilter = "all" | "now" | "today" | "categories" | "friends";
export function FigmaHomeFilters({ value, onChange }: { value: HomeFilter; onChange: (value: HomeFilter) => void }) { const filters: [HomeFilter,string][] = [["now","Agora"],["today","Hoje"],["categories","Categorias"],["friends","Amigos"]]; return <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.row}>{filters.map(([id,label]) => <Pressable key={id} onPress={() => onChange(value === id ? "all" : id)} style={[styles.chip, value === id && styles.active]}><Text style={[styles.text, value === id && styles.activeText]}>{label}</Text></Pressable>)}</ScrollView>; }
const styles = StyleSheet.create({ row: { gap: 7, paddingLeft: 2, paddingTop: 10 }, chip: { height: 32, paddingHorizontal: 14, borderRadius: 999, justifyContent: "center", backgroundColor: "rgba(21,25,29,.93)", borderWidth: 1, borderColor: "rgba(248,250,252,.06)" }, active: { backgroundColor: colors.brand, borderColor: colors.brand }, text: { color: colors.text, fontFamily: fonts.medium, fontSize: 13 }, activeText: { color: colors.background, fontFamily: fonts.bold } });
