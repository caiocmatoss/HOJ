export type VisualCategory = "bar" | "restaurant" | "club" | "lounge" | "show" | "cinema" | "event";
export const VISUAL_CATEGORIES: Record<VisualCategory, { color: string; emoji: string }> = {
  bar: { color: "#F5C542", emoji: "🍷" }, restaurant: { color: "#E09F3E", emoji: "🍽️" }, club: { color: "#C97B5A", emoji: "🎶" }, lounge: { color: "#B88A6A", emoji: "🍸" }, show: { color: "#9B8FCC", emoji: "🎸" }, cinema: { color: "#6897CC", emoji: "🎬" }, event: { color: "#6ABFA0", emoji: "🎪" },
};
export function normalizeVisualCategory(value: string): VisualCategory { const normalized = value.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase(); if (normalized.includes("restaurante") || normalized.includes("gastronomia") || normalized.includes("comida")) return "restaurant"; if (normalized.includes("balada") || normalized.includes("festa") || normalized.includes("club")) return "club"; if (normalized.includes("lounge")) return "lounge"; if (normalized.includes("show") || normalized.includes("musica")) return "show"; if (normalized.includes("cinema")) return "cinema"; if (normalized.includes("evento") || normalized.includes("festival")) return "event"; if (normalized.includes("bar")) return "bar"; return "event"; }
