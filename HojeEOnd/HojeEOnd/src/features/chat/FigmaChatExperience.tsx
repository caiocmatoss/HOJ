/**
 * Entry point for the production chat experience.
 * The data, Socket.IO subscriptions and message actions remain centralized in
 * ChatInboxExperience so direct and group conversation flows stay intact.
 */
export { default } from "./ChatInboxExperience";
