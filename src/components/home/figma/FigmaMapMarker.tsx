import { Ionicons } from "@expo/vector-icons";
import { Pressable, StyleSheet, Text } from "react-native";
import { colors, fonts, shadows } from "@/theme/tokens";
import { VISUAL_CATEGORIES, normalizeVisualCategory } from "./visualCategories";
export function FigmaMapMarker({ category = "event", selected, clusterCount, onPress }: { category?: string; selected?: boolean; clusterCount?: number; onPress?: () => void }) { const cluster = clusterCount != null; const visual = VISUAL_CATEGORIES[normalizeVisualCategory(category)]; const color = cluster ? colors.brand : visual.color; return <Pressable accessibilityRole="button" accessibilityLabel={cluster ? `${clusterCount} locais no mapa` : "Local no mapa"} onPress={onPress} style={[styles.marker, { borderColor: color }, selected && styles.selected]}><Text style={[styles.glyph, cluster && styles.clusterText, { color }]}>{cluster ? clusterCount : visual.emoji}</Text></Pressable>; }
const styles = StyleSheet.create({ marker: { width: 38, height: 38, borderRadius: 19, alignItems: "center", justifyContent: "center", backgroundColor: colors.surface, borderWidth: 1.5, overflow: "hidden", ...shadows.floating }, selected: { width: 48, height: 48, borderRadius: 24 }, glyph: { fontFamily: "Apple Color Emoji, Segoe UI Emoji, Noto Color Emoji", fontSize: 17, lineHeight: 20, textAlign: "center", includeFontPadding: false }, clusterText: { fontFamily: fonts.bold, fontSize: 15, lineHeight: 18 } });
