import { useMutation,useQuery,useQueryClient } from '@tanstack/react-query'; import { apiClient,apiClientWithMeta } from '@/services/api/client'; import { parsePaginationHeaders,type PaginatedResult } from '@/services/api/pagination'; import { inviteKeys,groupKeys } from '@/services/api/query-keys';
export type InviteUser={id:string;name:string;avatar:string|null;status?:string}; export type Invite={id:string;groupId:string;senderId:string;receiverId:string;status:'PENDING'|'ACCEPTED'|'REJECTED';createdAt:string;respondedAt:string|null;group?:{id:string;name:string;venueId:string};sender?:InviteUser;receiver?:InviteUser}; type P={page?:number;limit?:number}; const qs=(p:P={})=>`page=${Math.max(1,p.page??1)}&limit=${Math.min(100,Math.max(1,p.limit??100))}`;
export async function listReceivedInvites(p:P={}):Promise<PaginatedResult<Invite>>{const r=await apiClientWithMeta<Invite[]>(`/invites?${qs(p)}`);return parsePaginationHeaders(r.data,r.headers)} export async function listSentInvites(p:P={}):Promise<PaginatedResult<Invite>>{const r=await apiClientWithMeta<Invite[]>(`/invites/sent?${qs(p)}`);return parsePaginationHeaders(r.data,r.headers)} export const sendInviteResource=(g:string,r:string)=>apiClient<Invite>(`/groups/${encodeURIComponent(g)}/invites`,{method:'POST',body:{receiverId:r}}); export const acceptInviteResource=(id:string)=>apiClient<unknown>(`/invites/${encodeURIComponent(id)}/accept`,{method:'PATCH'}); export const rejectInviteResource=(id:string)=>apiClient<Invite>(`/invites/${encodeURIComponent(id)}/reject`,{method:'PATCH'});
export function useReceivedInvitesQuery(p:P={}){return useQuery({queryKey:inviteKeys.received(p),queryFn:()=>listReceivedInvites(p)})} export function useSentInvitesQuery(p:P={}){return useQuery({queryKey:inviteKeys.sent(p),queryFn:()=>listSentInvites(p)})} export function useInviteMutation(action:'send'|'accept'|'reject'){const c=useQueryClient();return useMutation({mutationFn:(v:{id?:string;groupId?:string;receiverId?:string})=>action==='send'?sendInviteResource(v.groupId as string,v.receiverId as string):action==='accept'?acceptInviteResource(v.id as string):rejectInviteResource(v.id as string),onSuccess:()=>{void c.invalidateQueries({queryKey:inviteKeys.all});void c.invalidateQueries({queryKey:groupKeys.all})}})}